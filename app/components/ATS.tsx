import React from "react";

interface sugesstions {
  type: string;
  tip: string;
}
const ATS = ({
  feedback,
  sugesstions,
}: {
  feedback: number;
  sugesstions: sugesstions[];
}) => {
  return <div>ATS</div>;
};

export default ATS;
